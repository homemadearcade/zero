import React from 'react';
import { Divider } from '@mui/material';

// eslint-disable-next-line import/no-anonymous-default-export
export default (props) => {
  return (
    <Divider sx={{my: '1rem'}} {...props}/>
  );
};
